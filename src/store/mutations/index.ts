import {
  countUp,
  getSelectedMenu,
  setSampleText,
  setSelectedMenu,
  setUserId,
  setUserName,
} from './common';

import {
  setActiveMenuButton,
  setActiveNotificationButton,
} from './information';

import {
  setMembers,
  setFilterTeam,
} from './members';

export default {
  setSampleText,
  countUp,
  setUserName,
  setUserId,
  setSelectedMenu,
  getSelectedMenu,
  setActiveNotificationButton,
  setActiveMenuButton,
  setMembers,
  setFilterTeam,
};
