import {
  countUp,
  setSampleText,
  setUserId,
  setUserName,
} from './common';

import {
  setActiveMenuButton,
  setActiveNotificationButton,
} from './information';

export default {
  setSampleText,
  countUp,
  setUserName,
  setUserId,
  setActiveNotificationButton,
  setActiveMenuButton,
};
