import {
  countUp,
  setSampleText,
  setUserId,
  setUserName,
  setSelectedMenu,
  getSelectedMenu,
} from './common';

import {
  setActiveMenuButton,
  setActiveNotificationButton,
} from './information';

export default {
  setSampleText,
  countUp,
  setUserName,
  setUserId,
  setSelectedMenu,
  getSelectedMenu,
  setActiveNotificationButton,
  setActiveMenuButton,
};
