import {
  countUp,
  getSelectedMenu,
  setSampleText,
  setSelectedMenu,
  setUserId,
  setUserName,
} from './common';

import {
  setActiveMenuButton,
  setActiveNotificationButton,
} from './information';

export default {
  setSampleText,
  countUp,
  setUserName,
  setUserId,
  setSelectedMenu,
  getSelectedMenu,
  setActiveNotificationButton,
  setActiveMenuButton,
};
